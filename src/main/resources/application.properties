server.port=${SERVER_PORT:8080}
spring.datasource.url=${DATASOURCE_URL:jdbc:postgresql://db:5432/msngr}
spring.datasource.username=${DATASOURCE_USERNAME:msngradmin}
spring.datasource.password=${DATASOURCE_PASSWORD:1111}
spring.jpa.hibernate.ddl-auto=update
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:simplesn.bot@gmail.com}
spring.mail.password=${MAIL_PASSWORD:gfcudrjrpixdxefn}
spring.mail.properties.mail.smtp.starttls.enable=${STARTTLS_ENABLE:true}
email-service.domain=${EMAIL_SERVICE_DOMAIN:localhost}
email-service.port=${EMAIL_SERVICE_PORT:8080}
email-service.use-https=${EMAIL_SERVICE_USE_HTTPS:false}